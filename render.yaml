# Render Blueprint for Atlas Virtual Receptionist
services:
  - type: web
    name: virtual-receptionist
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production

